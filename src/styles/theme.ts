'use client';

import { createTheme } from "@mui/material";

const theme = createTheme({
	palette: {
		mode: 'light',
		primary: {
			main: 'rgba(255,49,163,0.87)',
		},
		secondary: {
			main: '#007ef5',
		},
	},
});


export default theme